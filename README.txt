Command:
python main.py
	to run to the main program, including perspective transformation closing, bilateral filter, 		
	histogram regularization (CLAHE), homomorphic filter, and inpainting

Command:
python classify.py
	to run the machine learning model which return the accuracy